/**
 * CECUREUS — Color Scheme Detection Hook (Native Platforms)
 *
 * Why this file was created:
 * This hook detects the device's native system appearance mode (light vs dark).
 * It was created to provide reactive theme updates on physical Android and iOS devices,
 * re-exporting React Native's native `useColorScheme` hook.
 */

export { useColorScheme } from 'react-native';

